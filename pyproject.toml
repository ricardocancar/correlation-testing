[project]
name = "inicadores"
version = "0.1.0"
description = "Add your description here"

readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "yfinance",
    "pandas",
    "numpy",
    "arch",
    "streamlit>=1.55.0",
    "pandas-datareader",
]
[tool.poetry]
package-mode = false